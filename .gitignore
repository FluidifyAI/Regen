# Mockup preview files — local only, never committed
frontend/mockups/
frontend/mockup.html
frontend/mockup.mp4
frontend/mockup.gif
frontend/.frames/
frontend/scripts/
frontend/src/mockup-main.tsx
frontend/src/components/ui/IncidentMockup.tsx
frontend/src/pages/MockupPreviewPage.tsx

# Environment files
.env
.env.local
.env.*.local

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work
/backend/tmp/
/backend/bin/

# Node
node_modules/
/frontend/dist/
/frontend/build/
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Docker
*.pid
.dockerignore

# OS
.DS_Store
Thumbs.db

# Git worktrees (local development only)
.claude/worktrees/

# Helm subchart tarballs (generated by helm dep update)
deploy/helm/fluidify-regen/charts/
.worktrees/

# Embedded frontend build output (generated by `npm run build` + Dockerfile)
# The .gitkeep inside ensures //go:embed compiles without a frontend build.
backend/ui/dist/*
!backend/ui/dist/.gitkeep

# Internal docs, assets, dev tooling — local only, never pushed to remote
/.internal/
/test-infra/
/ref/

# Demo deployment values (contains secrets — never commit)
/deploy/demo/

# Private internal docs (product vision, decisions, roadmap) — not for public repo
/docs/PRODUCT.md
/docs/ARCHITECTURE.md
/docs/DECISIONS.md
/docs/FEATURES.md

# Claude Code project instructions — internal dev playbook, not for public repo
/CLAUDE.md

# Generated artifacts
*.zip
*.tar.gz
*.pdf

# IDE / AI assistant config (local development only)
.claude/
.serena/

# Vite cache
frontend/.vite//backend/openincident
/backend/regen
